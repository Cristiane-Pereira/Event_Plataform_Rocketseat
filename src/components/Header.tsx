import { Logo } from "./Logo";

const Header = () => {
  return (
    <header className="w-full py-5 flex aling-center justify-center bg-gray-900 border-b border-gray-600">
      <Logo />
    </header>
  );
};

export default Header;
